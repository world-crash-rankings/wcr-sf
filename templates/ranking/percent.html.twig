{% extends 'base.html.twig' %}
{% import 'ranking/_ranking_macros.html.twig' as macros %}

{% block title %}% WR Ranking - {{ parent() }}{% endblock %}

{% block meta_description %}Percent World Record Ranking - Top {{ players|length }} Burnout 2 Crash Mode players by average percentage of world record{% if players[0] is defined %}. Leader: {{ players[0].name }} with {{ players[0].avgPercent }}% WR{% endif %}.{% endblock %}

{% block meta_keywords %}percent wr ranking, world record percentage, burnout 2 leaderboard{% endblock %}

{% block structured_data %}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "ItemList",
  "name": "Percent WR Ranking",
  "description": "Top Burnout 2 Crash Mode players ranked by average percent of world record",
  "numberOfItems": {{ players|length }},
  "itemListElement": [
    {% for player in players|slice(0, 10) %}
    {
      "@type": "ListItem",
      "position": {{ player.avgPercentRank }},
      "item": {
        "@type": "Person",
        "name": "{{ player.name }}",
        "url": "{{ url('player_view', {nameUrl: player.nameUrl}) }}"
      }
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
</script>
{% endblock %}

{% block body %}
{% set current_ranking = 'percent' %}
{% include 'ranking/_ranking_header.html.twig' %}

<div class="row">
    <div class="col-12">
        <table class="table table-striped table-hover">
            <tbody>
                {% for player in players %}
                    {{ macros.ranking_row(player, 'avgPercentRank', 'avgPercent', 'percent') }}
                {% endfor %}
            </tbody>
        </table>

        <div class="mt-3">
            {{ form_start(form) }}
                <div class="row align-items-end">
                    <div class="col-auto">
                        {{ form_row(form.limit) }}
                    </div>
                    <div class="col-auto">
                        {{ form_row(form.submit) }}
                    </div>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
